# 电动 BMS 项目长期规则（旧版 Cursor 兼容）

1. 开始工作时先检查当前 Git 分支，默认留在当前分支。未经用户明确同意，禁止创建、切换、重命名或删除分支，禁止创建或切换 worktree，也不得执行会改变分支的 `git checkout` 或 `git switch`。
2. 如果当前分支不是 `main` 且任务涉及发布，必须停止并询问用户。若工具已经自动切换分支，应立即报告，确认前不得提交、推送或发布。
3. 用户要求“打包发布”时，必须完成版本号和 `CHANGELOG.md`、测试、Lint、隐藏测试入口构建、Git 提交、`git push origin <当前发布分支>`、GitHub Release、服务器 `version.json` 和线上校验。只在本地提交不算发布成功。
4. Git 提交说明、改动摘要、PR 标题与正文、Release 标题与说明、更新记录全部使用中文，并写清实际修改内容。
5. GitHub Actions 必须等待最终成功。发布后验证远程提交、Release APK 的版本与签名，以及 `https://shishanling.cn/jbd-bms/version.json`。
6. 提交前检查 `git status`，只提交本次需求相关文件，不得混入用户的临时文件、日志、APK、`output/`、`releases/` 或 `tmp/`。
7. 最终答复应包含当前分支、提交编号、GitHub 推送结果、Release 地址、本地 APK 地址和服务器清单地址。
