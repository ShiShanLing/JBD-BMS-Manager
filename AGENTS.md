# 项目长期开发规则

本文件是本仓库的长期协作约束，适用于 Codex 及其子任务。开始修改、提交、打包或发布前必须遵守。

## 分支规则

- 开始工作时先读取当前分支并告知用户；默认继续使用当前分支。
- 禁止自行创建、切换、重命名或删除分支，也禁止自行创建或切换 Git worktree。
- 不得执行 `git checkout`、`git switch` 或其他会改变当前分支的操作，除非用户已经明确同意具体目标分支。
- 如果当前分支不是预期发布分支 `main`，必须停止发布并询问用户，不能假设可以切回 `main`，也不能把其他分支冒充线上版本。
- 如果开发工具或外部环境已经自动切换了分支，应立即向用户说明当前分支、原分支和检测到的差异，并在得到确认前不提交、不推送、不发布。

## 提交与推送

- Git 提交说明、改动摘要、Pull Request 标题与正文、GitHub Release 标题与更新说明必须使用中文。
- 提交前检查 `git status` 和差异，只提交本次需求相关文件，不得把用户的临时文件、安装包、日志或无关改动混入提交。
- 用户要求“打包发布”“发布版本”或同等含义时，流程必须包含：更新版本号和更新记录、验证构建、提交代码、推送当前发布分支到 GitHub、发布 APK、更新服务器版本清单、验证线上结果。
- 发布操作不能只在本地提交。提交成功后必须主动执行 `git push origin <当前发布分支>`，并核对 GitHub 远程分支已经包含本次提交。
- 如果远程推送失败，不得宣称发布完成；应继续排查，或明确告知用户阻塞原因。
- 如果只要求本地调试或预览，不得擅自发布；但一旦用户明确要求“发布”，远程推送是不可省略的步骤。

## 发布校验

- 正式 APK 必须隐藏测试入口，并执行单元测试、Lint 和正式发布参数构建。
- GitHub Actions 启动后，应等待最终任务完成并确认成功，不能仅凭代码已推送就宣布发布完成。
- 核对 GitHub Release 的版本号、Build、APK 文件名和签名；本地交付包应与 GitHub 最终资产保持一致。
- 核对 `https://shishanling.cn/jbd-bms/version.json` 的版本号、Build、APK 地址和强制更新状态。
- 最终答复必须明确给出：当前分支、提交编号、远程推送结果、GitHub Release 地址、本地 APK 地址、服务器清单地址及各项验证结果。

## 更新记录

- 开发中的功能先详细写入 `CHANGELOG.md` 的“未发布”部分。
- 发布时将“未发布”内容移动到对应版本，记录发布日期和 Build。
- 更新说明应使用中文，写清楚用户可见变化、数据兼容性和重要修复，不能只写模糊的“优化”或英文摘要。
