# 电动 BMS 更新记录

本文件记录每个版本面向用户的功能变化、重要优化、问题修复和兼容性调整。

维护规则：

- 开发中的变化先写入“未发布”。
- 发布时将内容移动到对应版本，并填写发布日期和内部版本号（Build/versionCode）。
- 每项说明应写清楚修改了什么、用户能看到什么变化，以及必要的兼容性影响。
- 已发布记录只补充说明，不删除历史版本。

## [未发布]

### 修复

- 修正发布脚本默认下载源被回退到服务器 `latest.apk` 的问题；应用内更新恢复使用 GitHub Releases APK，服务器仅保留轻量版本清单。

## [0.5.8] - 2026-08-26（Build 38）

### 新增

- 在线更新服务增加最低可更新版本限制：0.5.0 以下版本停止获取版本清单和服务器 APK，0.5.0 及以上版本保持正常更新。
- 新增“容量健康档案”，自动以 BMS 返回的总容量为基准记录实测放电Ah/Wh、SOH、循环次数、温度和备注，并展示长期趋势。
- 顶层“里程”调整为“历史”，内部使用“行程记录 / 容量健康”二级切换，为后续曲线和告警历史预留空间。
- “最后一次状态”完整保存概览、保护参数、续航测试和历史数据；蓝牙断开后仍可通过相同的四个标签离线查看。
- “历史”新增“告警记录”，自动保存 App 在线期间 BMS 实际上报的保护触发与解除，并记录当时的 SOC、电压、电流、温度、最低单体和压差。
- 新增电池高温安全警告：结合 BMS 高温保护阈值、最高探头温度和一分钟升温速度，在危险时发送高优先级声音震动通知，并在 App 前台显示警告弹框。
- 红色危险温度支持锁屏亮屏全屏警报和覆盖其他 App 的全屏悬浮警报；两项均由用户在设备设置中单独授权，未授权时自动降级为普通高优先级通知。
- “历史”新增“趋势”页面：连接期间每10秒记录总压、电流、SOC、最高温度、单体压差和最低单体，支持查看本次行程、1小时、24小时、7天和30天曲线，蓝牙断开后仍可离线查看。
- 新增满充单体长期对比：SOC达到98%以上时，每天自动保存总压最高的一次逐串电压；记录长期保留，可对比同一串电池跨月、跨年的电压变化。
- “历史 → 容量”新增电池健康诊断：结合真实容量测试、可比满充单体记录、异常单体欠压和温度保护，给出正常、观察、警告或严重等级及诊断可信度。
- 新增“数据管理”：支持通过系统文件选择器创建完整本地备份、校验并恢复备份，不需要申请额外存储权限。
- 新增CSV资料包导出，分别包含趋势明细、每日摘要、满充逐串电压、容量测试、保护告警、骑行记录和分速度续航样本。
- 新增电池健康PDF报告：汇总当前关键数据、健康诊断、实测容量趋势、满充逐串长期对比、保护异常证据和判断条件，可通过系统文件选择器保存或分享。
- 健康PDF报告支持直接在App内逐页预览、双指缩放和拖动；预览文件仅存于缓存，关闭后自动删除，并可在预览页选择保存或分享。
- 新增自动容量测试：满电且 SOC 不低于 98% 时自动开始，根据 BMS 实时电流累计放电 Ah/Wh，到达低电量或低压保护后自动完成，也支持手动开始和结束。

### 优化

- 历史分类子页面放大左上角返回箭头及点击区域，提升单手操作和手机支架场景下的可点按性。
- 行程、告警、容量、趋势和诊断改为独立全屏子页面；进入后隐藏设备头部和底部主导航，仅保留子页面返回入口，返回后恢复原来的历史分类位置。
- “历史”页面顶部固定为“记录 / 健康 / 数据”三个分类；行程、告警、容量、趋势、诊断和数据管理改用摘要卡片进入，避免功能增加后顶部标签持续变长。
- “数据”分类集中提供备份恢复、CSV原始资料和健康PDF入口；“健康”分类独立提供容量、趋势和诊断入口，最后一次离线状态同步使用相同结构。
- 欠压事件会结合 SOC、放电电流和单体压差，区分正常低电量截止、负载瞬时压降和疑似异常欠压。
- 详情页“概览 / 保护 / 续航 / 历史”改为带线性图标的固定底部导航，释放顶部空间并为后续功能扩展留出位置；小窗入口同步移至详情头部。
- 高温判断采用连续样本确认和恢复滞回，过滤单次温度抖动；BMS 已触发高温保护时则立即升级为红色危险警告。
- 趋势数据按设备地址隔离；近7天保留10秒精度，7～30天自动汇总为5分钟精度，30天后保留每日摘要，兼顾近期诊断精度、长期变化依据和本地空间占用。
- 单体健康诊断使用相对整组平均值的长期漂移，自动排除整组电压共同升降；仅比较SOC、温度、串数相近且至少间隔7天的记录，数据不足时不会强行判断。
- 完整恢复采用“先校验、再确认、后替换”流程；蓝牙连接或扫描期间禁止恢复，数据库替换失败时自动保留原数据。
- 健康报告完全在手机本地生成，不上传电池数据；报告会明确标注其属于只读监测资料，不能替代厂商或法定检测机构的容量鉴定。
- 自动容量测试状态会持久保存并在蓝牙重连后继续；根据起止 SOC、低压截止、断线数据覆盖率和途中充电量区分“正式结果”与“参考结果”，参考结果不参与健康诊断。

### 修复

- 修复在“历史 → 数据”打开健康PDF后，点击关闭会丢失当前导航状态并返回概览页面的问题；PDF现在覆盖在原页面上显示，关闭后保持原位置。

## [0.5.7] - 2026-08-26（Build 37）

### 新增

- “最后一次状态”新增“概览 / 续航测试 / 行程”标签，可离线查看保存时的电池状态、分速度续航估算、里程图表和日历。
- 最后状态快照同步保存完整里程历史与各速度档长期续航样本，离开电池或蓝牙断开后仍可查看。

### 优化

- 灵动岛保留现有电量图标，紧凑文字只显示 SOC 百分比（例如 `52%`），减少占用宽度。

### 修复

- 修复新的 0 公里行程覆盖最近有效行程，导致历史行程卡片错误显示为 0 的问题。
- 修复蓝牙意外掉线和自动重连时最高时速被清零的问题；现在仅在手动断开、一键退出或开始全新行程时重置。

## [0.5.6] - 2026-08-22（Build 36）

### 新增

- 新增独立“里程”Tab，集中查看骑行历史。
- 新增每日里程日历，常驻显示每一天的骑行距离。
- 新增日、周、月、年四种里程柱状图。
- 新增今天、本周、本月、本年里程汇总。
- 新增里程历史本地存储，App 重启后仍可继续查看。
- 新增 CI 固定签名 APK 构建与 GitHub Release 发布流程，可覆盖安装历史版本。

### 优化

- 概览页面和画中画小窗增加今日总里程。
- 优化骑行画中画小窗布局和信息密度。
- 隐藏演示数据入口。

### 移除

- 移除刹车测试 Tab 及相关计算代码。

## [0.5.5] - 2026-08-22（Build 35）

### 新增

- 首次加入骑行里程统计页面。
- 增加日历热力图。
- 增加按日、周、月、年查看骑行里程的柱状图。

### 优化

- 调整骑行画中画小窗样式，使其与充电小窗风格一致。

## [0.5.4] - 2026-08-20（Build 34）

### 优化

- 移除设备列表中的预览测试数据入口，普通使用过程不再展示演示功能。
- 详情页面和“最后一次状态”页面支持系统返回键及返回手势。
- 返回操作改为回到上一页，不再直接退出到手机桌面。

## [0.5.3] - 2026-08-20（Build 33）

### 优化

- 更新弹窗增加跨版本更新说明。
- 当用户跳过多个版本直接升级时，会依次展示所有中间版本的更新内容，不再只显示最新版本说明。

## [0.5.2] - 2026-08-20（Build 32）

### 优化

- 应用名称统一为“电动 BMS”。
- 桌面图标名称、App 内标题和预览数据名称保持一致。

## [0.5.1] - 2026-08-20（Build 31）

### 优化

- 应用名称由带品牌限制的名称调整为更通用的“电动BMS”。
- 桌面图标使用短名称，App 内标题调整为“电动 BMS”。

## [0.5.0] - 2026-08-20（Build 30）

### 新增

- 新增画中画小窗。连接 BMS 后按 Home 键，或点击详情页画中画按钮即可进入。
- 画中画会根据车辆状态自动展示骑行或充电信息。
- 新增 BMS 保护参数只读页面，可查看过压、欠压、过流和温度等保护阈值。
- 新增保护参数解析、充电判断和续航估算相关测试。

### 优化

- 充电判断调整为车辆静止且充电电流大于 7A，减少将骑行中的动能回收误判为插枪充电的情况。
- 继续保持安全只读模式，不开放保护参数修改、MOS 控制或工厂模式。

## [0.4.7] - 2026-08-19（Build 17）

### 修复

- 将内部版本号恢复为递增的 17。
- 修复 0.4.6 内部版本号过低，导致 Android 将新 APK 判断为旧版本、无法覆盖安装的问题。
- 验证从 0.4.5 通过 App 内更新到新版本的流程。

## [0.4.6] - 2026-08-19（内部版本号 1，更新测试版）

### 测试

- 用于验证 App 内版本检测、下载和安装流程。

### 已知问题

- 内部版本号误设为 1，低于已安装版本，因此部分设备无法直接覆盖安装；该问题在 0.4.7 修复。

## [0.4.5] - 2026-08-19（Build 16）

### 优化

- APK 下载源由百度云服务器切换到 GitHub Releases，避免服务器 1 Mbps 带宽限制。
- 更新系统支持 HTTPS 下载和跳转安装。
- 发布脚本改为同时维护 GitHub Release 与服务器版本清单。

## [0.4.4] - 2026-08-19（Build 15，未单独发布 GitHub Release）

### 新增

- 新增 App 内版本检测、更新弹窗、APK 下载进度和系统安装调用。
- 新增服务器 `version.json` 更新清单。
- 新增“最后一次状态”本地保存：保存设备信息、电池基本信息、单体电压、GPS 时速和当前行程摘要。
- 新增首页历史图标，可在未连接电池时查看最后一次真实状态。
- App 在前台时保持屏幕常亮。

### 优化

- 断开蓝牙、退出 App、离开详情页或 App 进入后台时，用最新完整状态覆盖旧状态。
- 最后状态页面明确标记为离线数据，避免被误认为实时数据。

### 移除

- 移除通信日志 Tab、日志分享功能和最多 1000 条原始 BLE 报文缓存，减少不必要的内存占用。
- 保留协议自动识别、连接诊断、错误提示和自动重连，不影响正常蓝牙连接。

## [0.4.3] - 未正式发布

- 该版本号没有形成可独立追溯的 Git 提交或 GitHub Release。
- 相关开发变化随后统一并入 0.4.4。

## [0.4.2] - 2026-08-19（本地测试版，未发布 GitHub Release）

### 新增

- 验证“最后一次状态”本地保存与覆盖逻辑。
- 将最后状态入口移动到首页头部，并使用历史图标表示。
- 增加历史图标与连接状态提示之间的间距，降低误触概率。

### 优化

- 移除通信日志页面及底层日志缓存。
- 前台使用时保持屏幕常亮。
- 相关修改随后合并进 0.4.4 正式代码。

## [0.4.1] - 2026-08-18（Build 12）

### 重构

- 将原本集中的界面代码拆分为设备列表、概览、电池组件、续航测试、刹车测试和详情页面等独立模块。
- 将 GPS 行程、刹车测试、速度统计和本地状态存储拆分为独立功能模块。

### 新增

- 保存所有成功连接过的设备，并记录最后一次 SOC。
- 增加 GPS 近 5 秒平均速度和本次连接最高平均速度计算。
- 增加续航测试、速度分段累计和刹车测试相关测试。

### 优化

- 优化自动连接、意外断线重连和设备列表状态展示。
- 优化概览、单体电压、GPS 行程和续航测试页面布局。
- 保持 BMS 安全只读，不开放参数写入功能。

## [0.4.0] - 2026-08-18（GitHub Build 1，versionCode 11）

### 新增

- 新增连接 BMS 后自动开始的 GPS 行程统计。
- 记录行驶距离、SOC 变化、剩余容量变化、平均耗电和预计剩余续航。
- 新增按目标速度进行的独立续航测试，并保留长期累计样本。
- 新增 GPS 当前时速、近 5 秒平均时速和本次连接最高时速。
- 新增刹车距离测试，可按目标速度记录制动时间、距离、平均减速度和采样质量。
- 新增后台定位服务和常驻通知。
- 新增通知中的“结束行程”和“一键退出”能力。

### 优化

- 手动断开蓝牙时结束 GPS 行程和后台服务；意外掉线时允许自动重连，避免短暂断线切断行程。
- 完善 GPS、行程、续航和刹车测试的自动化测试。

## [0.3.2] - 2026-08-17～2026-08-18（versionCode 10，Build 6～16）

该版本连续发布了多个界面和连接体验测试包，版本名称保持 `0.3.2`，每个 Build 的变化如下。

### Build 16：应用品牌圆角优化

- App 头部应用 Logo 增加圆角裁切。
- Android Manifest 改为使用自适应应用图标和专用圆形图标。
- 改善桌面、系统设置和应用管理平台提取图标时的显示效果。

### Build 15：扫描动画

- 蓝牙扫描过程中刷新图标以每秒一圈的速度持续旋转。
- 停止扫描后图标自动停止旋转。
- 保持扫描中与未扫描状态使用不同颜色。

### Build 14：扩大扫描按钮点击区域

- 扫描按钮点击区域由 44dp 增大到 56dp。
- 扫描图标由 29dp 增大到 38dp，方便在手机支架等不易点击的场景下操作。

### Build 13：简化扫描页面

- 扫描按钮点击区域由 34dp 增大到 44dp，图标由 21dp 增大到 29dp。
- 删除“正在扫描附近的蓝牙设备”提示卡片，减少页面占用和重复提示。

### Build 12：刷新入口移入头部

- 增加统一的刷新图标入口，用于申请附近设备权限、开启蓝牙、开始扫描和停止扫描。
- 扫描时不再主动断开已连接的 BMS。
- 增加独立 `isScanning` 状态，使扫描状态与蓝牙连接状态可以同时存在。
- 删除扫描页原有的大按钮，简化设备扫描页面。

### Build 11：设备卡片支持进入详情

- 已连接设备的整张卡片可以点击进入详情页面。
- 保留卡片右侧连接/断开按钮，减少只能点击固定入口的限制。
- 点击未连接设备卡片不会误进入无数据详情页。

### Build 10：重连后自动进入详情

- 蓝牙从断线状态自动重连成功后，App 自动跳转到电池详情页面。
- 修复设备列表已经显示“已连接”，但偶尔仍停留在列表页的问题。

### Build 9：优化嘉佰达 BLE 通道选择

- 优先选择标准嘉佰达 `FF00/FF01/FF02` 服务和收发特征。
- 标准服务不可用时，自动尝试兼容的 `FFE0/FFE1` 通道。
- 两种已知通道都不可用时，再回退到自动探测可写和可通知特征。
- 增加 MTU 协商结果和 BLE 服务诊断信息。
- 未命名设备识别出 BMS 型号后，使用型号作为显示名称并保存。
- 自动连接时优先沿用历史设备名称，避免设备重新出现为“未命名设备”。

### Build 8：保存设备最后电量

- 成功读取基本信息后，将最新 SOC 保存到历史设备记录。
- 设备列表在已保存的蓝牙名称后显示最后一次电量百分比。
- 新数据会覆盖同一设备的旧 SOC，不重复创建保存记录。

### Build 7：完成详情页样式并移除预览入口

- 删除普通设备列表中的测试数据预览入口和演示模式入口。
- 完成概览和单体电压区域的紧凑布局。
- 优化总电压、剩余容量/总容量、压差、平均电压、循环次数和健康度卡片排列。
- 优化温度探头的单行自适应展示。
- 单体电压调整为每行五个，并突出最高、最低电压和均衡状态。
- 增加压差及电池健康度的分级颜色提醒。
- 统一充电 MOS、放电 MOS、电池均衡和电池保护的状态圆点逻辑与对齐方式。
- 继续保持只读监控，不增加保护参数修改功能。

### Build 6：详情页 UI 样式优化

- 重新设计电池概览页面的信息层级、字号、间距和卡片尺寸。
- 将电池状态、SOC、串数、生产日期和运行状态整合到顶部状态卡片。
- 将当前电流放到充电、放电或静置状态下方展示。
- 合并“剩余容量/总容量”，整数和小数按需显示，不再保留多余的零。
- 增加压差、平均单体电压、循环次数与健康度等重点信息卡片。
- 将单体电压内容合并到概览页面下方。
- 优化最高、最低单体电压的颜色标识和状态文字颜色。
- 删除无法直接从 BMS 获得的“本次充入/本次放出”展示参数。

## [0.3.1] - 2026-08-17（versionCode 9，Build 5）

### 新增

- 新增 BLE 连接诊断记录，保存发现的服务、写入特征和通知特征。
- 新增协议识别证据，区分标准 `DD…77`、V12 扩展状态和 `FF AA` 新版认证。
- 增加基本信息长度、串数、温度探头数量、软件版本和单体数据格式记录。
- 设备设置中增加 BLE 通道与连接诊断信息，便于确认实机采用的协议。

### 优化

- 扩充通信日志中的协议诊断内容，方便早期实机适配和故障定位。

## [0.3.0] - 2026-08-17（versionCode 8，Build 4）

### 新增

- 增加意外断线后的自动重连，依次按 2、5、10、30 秒退避尝试。
- 增加严格串行 BLE 命令队列，避免慢速蓝牙模块同时处理多条命令而丢失响应。
- 增加连接、服务发现、写入和协议响应超时。
- 超过 5 秒没有有效数据时标记为“数据过期”，超过 10 秒后主动断开并重连。
- 自动识别经典 JBD `DD…77` 状态帧和新版 `FF AA` 身份认证报文。
- 增加 6 位蓝牙只读密码认证支持。
- 增加新版认证报文加密、校验和协议解析测试。

### 优化

- 通信日志容量由最近 250 条扩大到 1000 条，便于早期协议诊断。
- 自动连接和手动断开状态分离，避免用户主动断开后立即再次重连。
- 增强非标准 BLE 服务与特征的自动发现兼容性。
- 明确旧数据的新鲜度，不再把已经停止更新的数据继续显示为“实时”。

## [0.2.5] - 2026-08-17（versionCode 7，初始版本 / Build 3）

仓库从 `0.2.5` 初始提交开始；GitHub 没有可独立追溯的 Build 1、Build 2 源码或 Release，最早保留的安装包记录为 Build 3。

### 初始功能

- 创建 Kotlin、Jetpack Compose 和 Android BLE 项目，最低支持 Android 12（API 31）。
- 扫描附近 BLE 设备，并优先标记可能属于嘉佰达的 BMS。
- 保存成功连接过的设备，App 再次打开时自动尝试连接最后使用的设备。
- 自动发现标准 `FF00/FF01/FF02`、兼容 `FFE0/FFE1` 以及未知可写/通知特征。
- 解析嘉佰达公开的 `DD…77` 只读协议。
- 读取硬件型号、芯片方案、基本状态和单体电压。
- 每秒刷新总电压、电流、剩余容量、总容量、SOC、循环次数、温度、MOS 和保护状态。
- 展示最高单体、最低单体、压差、平均电压和均衡状态。
- 支持部分 V12 扩展字段，包括湿度、报警、满充容量、均衡电流及估算 SOH。
- 增加设备设置弹窗，集中展示型号、芯片方案、软件版本和通信信息。
- 保存并分享最近 250 条原始通信日志，用于初期实机协议适配。
- 提供自适应图标、传统多密度 PNG 图标和专门设计的电池/BMS 主题图标。
- 从首个版本开始坚持安全只读，不开放保护参数写入、MOS 控制或工厂模式。

### 构建与发布

- 建立 GitHub Actions 自动测试、Lint、APK 构建和 Release 发布流程。
- Build 3 增加固定调试签名，保证云端生成的后续 APK 可以覆盖安装。
- 更新 GitHub Actions 运行环境版本，提升云端构建稳定性。
